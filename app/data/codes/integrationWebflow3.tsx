export default `import { ProductFilterWidget } from "@sledge-app/react-instant-search";

export default function ProductFilter() {
    return (
        <ProductFilterWidget
            data={{ collectionId: "value from your website" }}
        />
    );
}`